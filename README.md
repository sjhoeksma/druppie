# Druppie Architecture

Druppie is a spec-driven AI compliant with the Full Architecture described in `index.html`. This project allows for a full architectural description including building blocks, skills, build plane, runtime, and compliance.

## Overview

This project is designed to run directly from the file system. Open `index.html` in your browser to explore the architecture.
