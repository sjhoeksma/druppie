apiVersion: v1
kind: Service
metadata:
  name: {{ include "druppie.fullname" . }}
  labels:
    {{- include "druppie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "druppie.selectorLabels" . | nindent 4 }}
